Run `repo-compass scan` in the current working directory using Bash.

Then read the generated `CODEBASE.ctx` file.

You now have full codebase context. Confirm with one sentence: what this codebase is and what it does.
